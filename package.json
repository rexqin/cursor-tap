{
  "name": "cursor-tap-workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "nx run-many -t dev --projects=tap,api,web --parallel",
    "dev:tap": "nx dev tap",
    "dev:api": "nx dev api",
    "dev:web": "nx dev web",
    "build": "nx run-many -t build --all",
    "build:tap": "nx build tap",
    "build:api": "nx build api",
    "build:web": "nx build web",
    "test": "nx run-many -t test --all",
    "test:web": "nx test web",
    "e2e:web": "nx e2e web",
    "lint": "nx run-many -t lint --all"
  },
  "devDependencies": {
    "@naxodev/gonx": "3.0.0",
    "@nx/next": "23.0.0",
    "@nx/playwright": "^23.0.1",
    "@nx/vitest": "^23.0.1",
    "@playwright/test": "^1.61.1",
    "nx": "23.0.0"
  }
}
